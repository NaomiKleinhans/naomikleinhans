{
	"name": "user",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev -p 3007",
		"build": "npm run clean && next build",
		"start": "npm run build && next start -p 3607",
		"clean": "npx rimraf .next",
		"lint": "next lint"
	},
	"dependencies": {
		"react": "^18",
		"react-dom": "^18",
		"next": "14.2.3"
	},
	"devDependencies": {
		"postcss": "^8",
		"tailwindcss": "^3.4.1",
		"eslint": "^8",
		"eslint-config-next": "14.2.3"
	}
}
